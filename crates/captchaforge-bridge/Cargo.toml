[package]
name = "wafrift-captchaforge-bridge"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Optional wafrift adapter: lurien cookie wait for managed challenges. captchaforge is retired."
readme = "README.md"
keywords = ["wafrift", "captcha", "captchaforge", "cloudflare", "challenge"]
categories = ["network-programming", "web-programming"]

[dependencies]
wafrift-transport = { workspace = true }
# Firefox + BiDi driving via rustenium. Replaces the old
# `runtime_headless` + `chromiumoxide` CDP stack.
runtime-foxdriver = { path = "../../../browser/foxdriver", version = "0.1.5" }
guise = { workspace = true, features = ["browser"] }
tokio = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
async-trait = { workspace = true }
futures = { version = "0.3" }
serde_json = { workspace = true }
which = { version = "7" }

[lints]
workspace = true

[dev-dependencies]
temp-env = { version = "0.3.6", features = ["async_closure"] }
